const express = require("express");
const router = express.Router();
// const {teacherAuthenticate, studentAuthenticate } = require("../middleware/authenticate");
// const { createAttendanceRecord, findStudentAttenceRecordBySlotId } = require("../controllers/attendance.controller");
// router.post("/record/student/attendance",teacherAuthenticate,  createAttendanceRecord
// );
// router.get("/record/student/attendance",studentAuthenticate,findStudentAttenceRecordBySlotId
// );


module.exports = router;